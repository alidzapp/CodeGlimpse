Header = React.createClass({
  render() {
    return (
      <nav className="nav">
        <a href="/" className="logo">CG</a>
        <button className="menu-toggler">
          <span>Menu</span>
        </button>
        <a className="login">Login</a>
      </nav>
    );
  }
})
